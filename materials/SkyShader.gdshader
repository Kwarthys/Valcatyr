shader_type sky;

uniform sampler2D noise; // will have to create myself
// would love some cluster of stars milhyway style

void sky()
{
	// make nice shiny stars with custom made texture noise
	COLOR = vec3(0.0);
}
